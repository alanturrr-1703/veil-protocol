/**
 * Placeholder for client-side routing. The current build is a single-screen experience
 * orchestrated by App.tsx (lobby vs in-game), so no router is wired yet. Introduce
 * react-router here when multiple pages (profile, replays, spectate) are added.
 */
export {};
