package com.veil.domain.player;

/**
 * Win-condition alignment. Confidential: never broadcast; only revealed via
 * redacted, per-viewer projections (e.g. an Oracle's own investigation result).
 */
public enum Faction {
    SHADOW,
    CITY,
    NEUTRAL
}
